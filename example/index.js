var circ = require("./circle");
var radius = 5.0;

console.log("The area of a circle with radius " + radius + " is " + circ.area(radius));
console.log("and the circumference is " + circ.circumference(radius));